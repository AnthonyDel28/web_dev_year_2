{% extends 'admin/base.html.twig' %}
{% block title %}Modifier une formation{{ parent() }}{% endblock %}
{% block body %}
    <section class="row">
        <div class="col-md-8 offset-1">
            <h2 class="my-5 display-4">Modifier une formation</h2>
            {{ form_start(form) }}
            {{ form_widget(form) }}
            <button type="submit" class="btn btn-outline-secondary">Modifier la formation</button>
            {{ form_end(form) }}
        </div>
    </section>
{% endblock %}