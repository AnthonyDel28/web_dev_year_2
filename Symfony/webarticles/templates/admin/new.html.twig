{% extends 'base.html.twig' %}
{% block body %}
    <main class="container">
        <h3 class="display-5">Ajouter un article</h3>
        <div class="row">
            <div class="col-md-8 offset-1">
                {{ form_start(form) }}
                {{ form_widget(form) }}
                    <button class="btn btn-outline-secondary" type="submit">Ajouter l'article</button>
                {{ form_end(form) }}
            </div>
        </div>
    </main>
{% endblock %}